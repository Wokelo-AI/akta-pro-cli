"""Akta CLI — a command-line client for the Akta REST API (https://api.akta.pro).

A standalone client (its own `akta-cli` distribution) exposing the `akta`
console command; a sibling of the Akta MCP server over the same `/api/v1`
endpoints, with no MCP-server code.
"""

__version__ = "0.2.5"
